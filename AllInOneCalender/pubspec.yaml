name: all_in_one_calendar
description: A prototype iOS task manager for the AllInOneCalendar app built with Flutter.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  uuid: ^3.0.7

flutter:
  uses-material-design: true
  assets:
    - assets/
